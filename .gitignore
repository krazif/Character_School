# Database
char_test.db
char_test.db-*

# Python
__pycache__/
*.pyc
*.pyo

# Characters directory — keep the folder, ignore personal cards
# (default cards shipped with repo are force-added with git add -f)
characters/*.json
characters/*.png
# Ship p_ramlee as a default card
!characters/p_ramlee.json
!characters/p_ramlee.png

# Personal persona
personas/razif.json

# Environment / secrets
.env
*.log
config.jsonc

# OS files
.DS_Store
Thumbs.db
